# ==========================================
# 환경 변수 설정 파일 (예시)
# ==========================================
# 이 파일을 복사하여 .env.local 또는 .env로 사용하세요.
# Vite는 VITE_ 접두사가 붙은 변수만 클라이언트에 노출합니다.
#
# 사용법:
#   1. 이 파일을 복사: cp .env.example .env.local
#   2. 값을 환경에 맞게 수정
#   3. 개발 서버 재시작: npm run dev

# ==========================================
# API 설정
# ==========================================
# API 베이스 URL (개발 환경에서는 Vite proxy가 처리)
VITE_API_BASE_URL=/api

# 인증 관련 API 경로
VITE_AUTH_BASE_URL=/auth

# ==========================================
# 애플리케이션 설정
# ==========================================
# 앱 이름 (브라우저 탭 제목 등에 사용)
VITE_APP_NAME=SBS Application

# 앱 버전
VITE_APP_VERSION=1.0.0

# ==========================================
# 파일 업로드 설정
# ==========================================
# 프로필 이미지 최대 크기 (바이트 단위, 기본: 5MB = 5242880)
VITE_MAX_PROFILE_IMAGE_SIZE=5242880

# 배경 이미지 최대 크기 (바이트 단위, 기본: 10MB = 10485760)
VITE_MAX_BACKGROUND_IMAGE_SIZE=10485760

# 허용되는 이미지 타입 (콤마로 구분)
VITE_ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif,image/webp

# ==========================================
# OAuth 설정 (카카오 로그인)
# ==========================================
# OAuth 콜백 경로
VITE_OAUTH_CALLBACK_PATH=/oauth/callback

# ==========================================
# 개발 환경 전용 설정
# ==========================================
# 디버그 모드 (콘솔 로그 출력 여부)
VITE_DEBUG_MODE=true

# API 요청 로그 출력
VITE_LOG_API_REQUESTS=true
